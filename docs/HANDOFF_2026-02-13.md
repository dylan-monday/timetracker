# M+P Time Tracker - Handoff (Feb 13, 2026)

## Status
- Branch: `main`
- Latest deployed commit: pending push
- Production: https://time.mondayandpartners.com

## Completed This Session

### 1. Fixed mobile time entry UX
- Input field now closes immediately when tapping iOS keyboard "Done" button
- Visual save confirmation (green checkmark) appears briefly after successful entry
- Fixed bug where early return in submit handler left input field open
- Added `enterKeyHint="done"` for better iOS keyboard integration

### 2. New time-based gradient background
Replaced the failed blur/orb approach with CSS radial-gradients that shift based on time of day:
- **Morning (6am-11am)**: Cool blues, light teals, clean whites
- **Midday (11am-2pm)**: Warm whites, subtle golds, light sage
- **Afternoon (2pm-5pm)**: Soft amber, warm gray, muted gold
- **Evening (5pm-9pm)**: Warm peach, dusty rose, warm sand
- **Night (9pm-6am)**: Blue-gray, slate, muted indigo

Key features:
- Gradients slowly drift position over 30-60 second cycles
- 30-minute smooth color transitions between time periods
- Debug mode: `?debug-bg=1` shows full 24-hour cycle in 60 seconds
- Respects `prefers-reduced-motion`

### 3. Today column visual distinction
- Today's column has a subtle warm amber tint with "today" label
- Past days appear slightly muted/settled
- Future days appear lighter/more open
- CSS only, no animation needed

### 4. Language cleanup (warm, human tone)
Rewrote all user-facing strings:
- "No lines yet..." → "Clean slate. Add a project line to start capturing your day."
- "Full Day" → "Good day" | "X h open" → "X h to go"
- "Calendar drafts" → "Calendar imports"
- "Admin Mode" → "Settings" (including nav)
- "Do your time" (email) → "Quick snapshot of your [day name]"
- "Weekly time recap" → "Your week, wrapped"
- Updated trends labels: "Client delivery", "Internal investment"

### 5. Personal time categories
Added SQL migration and updated bootstrap function to create default Personal projects:
- Thinking & Strategy
- Recharging
- Exercise
- Personal Development
- Reading

Run migration: `supabase/migrations/2026-02-13_add_personal_time_projects.sql`

### 6. Removed debug build tag from login screen

## Backlog (Carried Forward)
1. Calendar drafts workflow: nothing should remain unmapped.
   - Required state: each draft must be either mapped to a valid project, or rejected.
   - No "unmapped skipped" end-state in normal usage.
2. Admin projects: archive icon pill should be tighter (less horizontal padding).

## Current Calendar Sync Behavior
- Sync no longer crashes on unmapped events.
- Current behavior: unmapped events are skipped and reported in sync message (`Skipped X unmapped events`).
- This was added to avoid DB errors where `time_entries.project_id` is non-nullable.

## Relevant Files
- Time entry grid (mobile + desktop): `components/week-grid.tsx`
- Calendar sync API: `app/api/calendar/sync/route.ts`
- Admin projects UI: `app/admin/page.tsx`
- Trends rates fallback: `app/trends/page.tsx`
- Shared week data loader fallbacks: `lib/supabase/week.ts`
- Visual background system: `app/globals.css`, `components/app-shell.tsx`, `components/auth-gate.tsx`

## DB Notes
If not already applied in Supabase, these columns are expected by newer rate features:
```sql
alter table public.clients add column if not exists hourly_rate_cents integer;
alter table public.projects add column if not exists hourly_rate_cents integer;
```

## Product Direction Reminder
- This is a personal + professional optimization tool.
- Priority is low-friction daily use and clarity, not accounting rigidity.
